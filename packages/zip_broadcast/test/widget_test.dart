// Scaffold test — replaced in Unit 3.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('zip_broadcast app stub is instantiable', (tester) async {
    expect(true, isTrue);
  });
}
